import { ele, txt, } from '/web/butterfly/src/butterfly.js';
import makeScrollList from './scrolling-list.js';

export default [
	makeScrollList( { classes:[ 'grid-item', 'refs', ], }, [
		
	], ),
];
